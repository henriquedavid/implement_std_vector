template < typename T >
class MyIterator {
	private:
		T *current;

	public:
		MyIterator();
		// const Object & operator*() const;
		// MyIterator & operator++ ( );
		// MyIterator operator++ ( int );
		// MyIterator & operator-- ( );
		// MyIterator operator-- ( int );
		// bool operator== ( const MyIterator & rhs ) const;
		// bool operator!= ( const MyIterator & rhs ) const;
};